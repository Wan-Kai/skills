# Skill Conventions

- Put every skill in `skills/<skill-name>/SKILL.md`.
- Use a short, action-oriented `description` in the frontmatter.
- Prefer linking to scripts or references instead of pasting long instructions.
- Keep examples concrete and minimal.
