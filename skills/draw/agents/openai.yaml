interface:
  display_name: "Draw"
  short_description: "Create editable draw.io diagrams with safe routing"
  default_prompt: "Use $draw to create an editable draw.io domain map with collision-free cross-domain routing."
